<?xml version="1.0" encoding="utf-8" ?>
<ResourceDictionary xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
                    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
                    xmlns:telerik="http://schemas.telerik.com/2022/xaml/maui"
                    xmlns:telerikMaui="clr-namespace:Telerik.Maui;assembly=Telerik.Maui.Core"
                    x:Class="Telerik.Theming.Styles.Chat">

    <!-- This is autogenerated file. Use as reference. Do not modify. -->

    <telerik:StringToBoolConverter x:Key="ChatStringToBoolConverter" />
    <telerik:MultiBooleanConverter x:Key="ChatIsFooterVisibleConverter" LogicalOperator="Or" />

    <Style x:Key="ChatIncomingTextMessageViewStyle" TargetType="telerik:IncomingTextMessageView">
        <Setter Property="LabelStyle">
            <Style TargetType="Label">
                <Setter Property="TextColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
            </Style>
        </Setter>
        <Setter Property="BorderStyle">
            <Style TargetType="telerik:RadBorder">
                <Setter Property="BackgroundColor" Value="{DynamicResource RadAppSurfaceColor}" />
            </Style>
        </Setter>
    </Style>
    <Style TargetType="telerik:IncomingTextMessageView" BasedOn="{StaticResource ChatIncomingTextMessageViewStyle}" ApplyToDerivedTypes="True" />

    <Style x:Key="ChatOutgoingTextMessageViewStyle" TargetType="telerik:OutgoingTextMessageView">
        <Setter Property="LabelStyle">
            <Style TargetType="Label">
                <Setter Property="TextColor" Value="{DynamicResource RadOnPrimaryColor}" />
            </Style>
        </Setter>
        <Setter Property="BorderStyle">
            <Style TargetType="telerik:RadBorder">
                <Setter Property="BackgroundColor" Value="{DynamicResource RadPrimaryColor}" />
            </Style>
        </Setter>
    </Style>
    <Style TargetType="telerik:OutgoingTextMessageView" BasedOn="{StaticResource ChatOutgoingTextMessageViewStyle}" ApplyToDerivedTypes="True" />

    <Style x:Key="ChatTimeBreakViewStyle" TargetType="telerik:TimeBreakView">
        <Setter Property="TextColor" Value="{DynamicResource RadSubtleColor}" />
        <Setter Property="Stroke" Value="{DynamicResource RadBorderColor}" />
        <Setter Property="StrokeThickness" Value="0" />
    </Style>
    <Style TargetType="telerik:TimeBreakView" BasedOn="{StaticResource ChatTimeBreakViewStyle}" />

    <Style x:Key="ChatTypingIndicatorStyle" TargetType="telerik:TypingIndicator">
        <Setter Property="TextColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
        <Setter Property="DotsColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
    </Style>
    <Style TargetType="telerik:TypingIndicator" BasedOn="{StaticResource ChatTypingIndicatorStyle}" />

    <Style TargetType="telerik:ChatEntry" BasedOn="{StaticResource RadEntryStyle}" />

    <Style x:Key="ChatSuggestedActionItemStyle" TargetType="telerik:RadButton" BasedOn="{StaticResource RadPrimaryOutlineButtonStyle}">
        <Setter Property="Padding" Value="12, 8" />
        <Setter Property="CornerRadius" Value="18" />
    </Style>

    <DataTemplate x:Key="ChatSuggestedActionItemTemplate">
        <telerik:RadButton Text="{Binding Text}"
                           Command="{Binding Command}"
                           Style="{StaticResource ChatSuggestedActionItemStyle}" 
                           AutomationId="SuggestedActionButton" />
    </DataTemplate>

    <DataTemplate x:Key="ChatSuggestedActionsTemplate">
        <telerik:SuggestedActionsView ItemsSource="{Binding Actions}"
                                      ItemTemplate="{DynamicResource ChatSuggestedActionItemTemplate}"
                                      SelectionMode="None"
                                      Orientation="Horizontal">
            <telerik:SuggestedActionsView.ItemsLayoutTemplate>
                <DataTemplate>
                    <HorizontalStackLayout Padding="{OnPlatform Default='0, 10', WinUI=10, MacCatalyst='12, 10'}"
                                           Spacing="4" />
                </DataTemplate>
            </telerik:SuggestedActionsView.ItemsLayoutTemplate>
        </telerik:SuggestedActionsView>
    </DataTemplate>
    <telerik:ChatItemTemplateSelector x:Key="ChatItemTemplateSelector"
                                      SuggestedActionsTemplate="{StaticResource ChatSuggestedActionsTemplate}" />

    <Style x:Key="ChatCardLabelStyle" TargetType="Label">
        <Setter Property="TextColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
    </Style>

    <Style x:Key="ChatBasicCardViewStyle" TargetType="telerik:BasicCardView">
        <Setter Property="ContentBackgroundColor" Value="{DynamicResource RadAppSurfaceColor}" />
        <Setter Property="TitleStyle" Value="{StaticResource ChatCardLabelStyle}" />
        <Setter Property="SubtitleStyle" Value="{StaticResource ChatCardLabelStyle}" />
        <Setter Property="DescriptionStyle" Value="{StaticResource ChatCardLabelStyle}" />
    </Style>

    <DataTemplate x:Key="ChatCardActionTemplate">
        <telerik:CardActionView x:Name="view"
                                Text="{Binding Text}"
                                Command="{Binding Command}"
                                HeightRequest="30" />
    </DataTemplate>

    <Style TargetType="telerik:CardActionView">
        <Setter Property="TextColor" Value="{DynamicResource RadPrimaryColor}" />
    </Style>

    <Style TargetType="telerik:BasicCardView" BasedOn="{StaticResource ChatBasicCardViewStyle}" ApplyToDerivedTypes="True">
        <Setter Property="ControlTemplate">
            <ControlTemplate>
                <telerik:RadBorder x:Name="PART_MainContainer"
                                  BorderBrush="{DynamicResource RadBorderColor}"
                                  BorderThickness="1"
                                  CornerRadius="4"
                                  WidthRequest="280"
                                  AutomationId="RadChatBasicCard">
                    <Grid RowDefinitions="Auto, Auto">
                        <VerticalStackLayout Padding="16, 12">
                            <Label Text="{TemplateBinding Title}"
                                   Style="{TemplateBinding ActualTitleStyle}"
                                   IsVisible="{TemplateBinding Title, Converter={StaticResource ChatStringToBoolConverter}}"
                                   AutomationId="RadChatCardTitle" />
                            <Label Text="{TemplateBinding Subtitle}"
                                   Style="{TemplateBinding ActualSubtitleStyle}"
                                   IsVisible="{TemplateBinding Subtitle, Converter={StaticResource ChatStringToBoolConverter}}"
                                   AutomationId="RadChatCardSubtitle" />
                            <Label Text="{TemplateBinding Description}"
                                   Style="{TemplateBinding ActualDescriptionStyle}"
                                   IsVisible="{TemplateBinding Description, Converter={StaticResource ChatStringToBoolConverter}}"
                                   AutomationId="RadChatCardDescription" />
                        </VerticalStackLayout>
                        <telerik:RadBorder Grid.Row="1"
                                                       BorderColor="{DynamicResource RadBorderColor}"
                                                       BorderThickness="0, 1, 0, 0">
                            <telerik:CardActionsListView ItemsSource="{TemplateBinding Actions}"
                                                         ItemTemplate="{StaticResource ChatCardActionTemplate}"
                                                         AutomationId="RadChatCardSelectButton" />
                        </telerik:RadBorder>
                    </Grid>
                </telerik:RadBorder>
            </ControlTemplate>
        </Setter>
    </Style>

    <Style TargetType="telerik:ImageCardView" BasedOn="{StaticResource ChatBasicCardViewStyle}" ApplyToDerivedTypes="True">
        <Setter Property="ControlTemplate">
            <ControlTemplate>
                <telerik:RadBorder x:Name="PART_MainContainer"
                                   BorderBrush="{DynamicResource RadBorderColor}"
                                   BorderThickness="1"
                                   CornerRadius="4"
                                   WidthRequest="280"
                                   AutomationId="RadChatImageCard">
                    <Grid RowDefinitions="Auto, *, Auto">
                        <Image Source="{TemplateBinding Image}"
                               VerticalOptions="FillAndExpand"
                               Aspect="Fill"
                               MaximumHeightRequest="160" />
                        <Grid Grid.Row="1"
                              Padding="16, 12"
                              RowDefinitions="Auto, *"
                              RowSpacing="12">
                            <VerticalStackLayout>
                                <Label Text="{TemplateBinding Title}"
                                       Style="{TemplateBinding ActualTitleStyle}"
                                       IsVisible="{TemplateBinding Title, Converter={StaticResource ChatStringToBoolConverter}}" />
                                <Label Text="{TemplateBinding Subtitle}"
                                       Style="{TemplateBinding ActualSubtitleStyle}"
                                       IsVisible="{TemplateBinding Subtitle, Converter={StaticResource ChatStringToBoolConverter}}" />
                            </VerticalStackLayout>
                            <Label Grid.Row="1"
                                   Text="{TemplateBinding Description}"
                                   LineBreakMode="WordWrap"
                                   MaxLines="4"
                                   Style="{TemplateBinding ActualDescriptionStyle}"
                                   IsVisible="{TemplateBinding Description, Converter={StaticResource ChatStringToBoolConverter}}" />
                        </Grid>
                        <telerik:RadBorder Grid.Row="2"
                                           BorderColor="{DynamicResource RadBorderColor}"
                                           BorderThickness="0, 1, 0, 0">
                            <telerik:CardActionsListView ItemsSource="{TemplateBinding Actions}"
                                                         ItemTemplate="{StaticResource ChatCardActionTemplate}" />
                        </telerik:RadBorder>
                    </Grid>
                </telerik:RadBorder>
            </ControlTemplate>
        </Setter>
    </Style>

    <Style x:Key="ChatPickerItemStyle" TargetType="telerik:NonVirtualizedItemsControlItemContainer">
        <Setter Property="BackgroundColor" Value="{DynamicResource RadAppSurfaceColor}" />
        <Setter Property="MinimumHeightRequest" Value="{OnPlatform Default=28, Android=40, iOS=40}" />
        <Setter Property="Padding" Value="{OnPlatform Default='8, 4', Android='8, 10', iOS='8, 10'}" />
        <Setter Property="TextMargin" Value="0" />
    </Style>

    <Style x:Key="ChatPickerSelectedItemStyle" TargetType="telerik:NonVirtualizedItemsControlItemContainer" BasedOn="{StaticResource ChatPickerItemStyle}">
        <Setter Property="BackgroundColor" Value="{DynamicResource RadBaseHoverColor}" />
    </Style>

    <DataTemplate x:Key="ItemChatPickerTemplate">
        <telerik:ItemPickerView x:Name="view"
                                WidthRequest="{OnPlatform MacCatalyst=260, WinUI=296}"
                                HorizontalOptions="{OnIdiom Default=Fill, Desktop=Start}"
                                ItemsSource="{Binding ItemsSource}"
                                ItemStyle="{StaticResource ChatPickerItemStyle}"
                                SelectionMode="{Binding SelectionMode}"
                                SelectedItem="{Binding SelectedItem, Mode=TwoWay}"
                                SelectedItems="{Binding SelectedItems}"
                                SelectedItemStyle="{StaticResource ChatPickerSelectedItemStyle}"
                                AutomationId="RadChatItemPicker" />
    </DataTemplate>

    <DataTemplate x:Key="CardChatPickerTemplate">
        <telerik:CardPickerView x:Name="view"
                                Background="{DynamicResource RadAppSurfaceColor}"
                                ItemsSource="{Binding Cards}"
                                SelectionMode="None"
                                AutomationId="RadChatBasicCardsList" />
    </DataTemplate>

    <DataTemplate x:Key="DateChatPickerTemplate">
        <telerik:DatePickerView x:Name="view"
                                Background="{DynamicResource RadAppSurfaceColor}"
                                BorderThickness="0"
                                CornerRadius="0"
                                MinDate="{Binding MinDate}"
                                MaxDate="{Binding MaxDate}"
                                SelectedDate="{Binding SelectedDate, Mode=TwoWay}"
                                HorizontalOptions="{OnIdiom Default=Fill, Desktop=Start}"
                                VerticalOptions="{OnIdiom Default=Fill, Desktop=Start}"
                                AutomationId="RadChatDatePicker" />
    </DataTemplate>

    <DataTemplate x:Key="TimeChatPickerTemplate">
        <telerik:TimePickerView x:Name="view"
                                WidthRequest="{OnPlatform MacCatalyst=260, WinUI=296}"
                                HorizontalOptions="{OnIdiom Default=Fill, Desktop=Start}"
                                StartTime="{Binding StartTime}"
                                EndTime="{Binding EndTime}"
                                TimeInterval="{Binding TimeInterval}"
                                SelectedValue="{Binding SelectedValue, Mode=TwoWay}" />
    </DataTemplate>

    <ControlTemplate x:Key="TimeChatPickerViewTemplate">
        <telerik:TimePickerListView ItemsSource="{TemplateBinding Items}"
                                    ItemStyle="{StaticResource ChatPickerItemStyle}"
                                    SelectedItem="{TemplateBinding SelectedItem, Mode=TwoWay}"
                                    SelectedItemStyle="{StaticResource ChatPickerSelectedItemStyle}"
                                    AutomationId="RadChatTimePicker">
            <telerik:TimePickerListView.ItemTemplate>
                <DataTemplate>
                    <telerik:TimePickerItemLabel TextColor="{DynamicResource RadOnAppSurfaceColor}"
                                                 Time="{Binding Time}"
                                                 TimeFormat="{TemplateBinding TimeFormat}"
                                                 VerticalOptions="Center"
                                                 AutomationId="RadChatTimePickerItem" />
                </DataTemplate>
            </telerik:TimePickerListView.ItemTemplate>
        </telerik:TimePickerListView>
    </ControlTemplate>

    <Style TargetType="telerik:TimePickerView">
        <Setter Property="ControlTemplate" Value="{StaticResource TimeChatPickerViewTemplate}" />
    </Style>

    <telerik:ChatPickerTemplateSelector x:Key="ChatPickerTemplateSelector"
                                        ItemPickerTemplate="{StaticResource ItemChatPickerTemplate}"
                                        DatePickerTemplate="{StaticResource DateChatPickerTemplate}"
                                        TimePickerTemplate="{StaticResource TimeChatPickerTemplate}"
                                        CardPickerTemplate="{StaticResource CardChatPickerTemplate}" />

    <ControlTemplate x:Key="RadChatPickerControlTemplate">
        <telerik:RadBorder BackgroundColor="{DynamicResource RadAppSurfaceColor}"
                           BorderColor="{DynamicResource RadBorderColor}"
                           BorderThickness="1"
                           HorizontalOptions="{OnIdiom Default=Fill, Desktop=Start}"
                           Margin="{OnPlatform Default='0, 0, 0, 10', WinUI='0, 0, 0, 12'}">
            <Grid RowDefinitions="Auto, *, Auto">
                <telerik:RadBorder x:Name="PART_Header"
                                   IsVisible="{TemplateBinding HeaderText, Converter={StaticResource ChatStringToBoolConverter}}"
                                   BorderColor="{DynamicResource RadBorderColor}"
                                   BorderThickness="0, 0, 0, 1"
                                   Padding="{OnPlatform Android='16, 15', iOS='16, 13.5', MacCatalyst='6, 7.5', WinUI='16, 14'}">
                    <Label x:Name="PART_HeaderLabel"
                           Text="{TemplateBinding HeaderText}"
                           TextColor="{DynamicResource RadOnAppSurfaceColor}"
                           LineBreakMode="WordWrap"
                           IsVisible="{TemplateBinding HeaderText, Converter={StaticResource ChatStringToBoolConverter}}" />
                </telerik:RadBorder>
                <ContentPresenter Grid.Row="1" />
                <telerik:RadBorder x:Name="PART_Footer"
                                   Grid.Row="2"
                                   BorderColor="{DynamicResource RadBorderColor}"
                                   BorderThickness="0, 1, 0, 0"
                                   Padding="8">
                    <telerik:RadBorder.IsVisible>
                        <MultiBinding Converter="{StaticResource ChatIsFooterVisibleConverter}">
                            <Binding Path="IsCancelButtonVisible" Source="{RelativeSource TemplatedParent}" />
                            <Binding Path="IsOkButtonVisible" Source="{RelativeSource TemplatedParent}" />
                        </MultiBinding>
                    </telerik:RadBorder.IsVisible>
                    <Grid ColumnDefinitions="*, *"
                          ColumnSpacing="4">
                        <telerik:RadButton x:Name="PART_CancelButton"
                                           Text="Cancel"
                                           IsVisible="{TemplateBinding IsCancelButtonVisible}"
                                           Style="{DynamicResource RadButtonStyle}"
                                           Command="{TemplateBinding ActualCancelCommand}"
                                           AutomationId="RadChatPickerCancelButton" />
                        <telerik:RadButton x:Name="PART_OkButton"
                                           Grid.Column="1"
                                           Text="Ok"
                                           IsVisible="{TemplateBinding IsOkButtonVisible}"
                                           Style="{DynamicResource RadPrimaryButtonStyle}"
                                           Command="{TemplateBinding ActualOkCommand}"
                                           AutomationId="RadChatPickerOkButton" />
                    </Grid>
                </telerik:RadBorder>
            </Grid>
        </telerik:RadBorder>
    </ControlTemplate>
    <Style x:Key="RadChatPickerStyle" TargetType="telerik:RadChatPicker">
        <Setter Property="ControlTemplate" Value="{StaticResource RadChatPickerControlTemplate}" />
        <Setter Property="TemplateSelector" Value="{StaticResource ChatPickerTemplateSelector}" />
        <Setter Property="HeaderTextColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
        <Setter Property="HeaderBackgroundColor" Value="{DynamicResource RadAppSurfaceColor}" />
        <Setter Property="FooterBackgroundColor" Value="{DynamicResource RadAppSurfaceColor}" />
        <Setter Property="OkButtonTextColor" Value="{DynamicResource RadOnPrimaryColor}" />
        <Setter Property="CancelButtonTextColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
    </Style>
    <Style TargetType="telerik:RadChatPicker" BasedOn="{StaticResource RadChatPickerStyle}" />

    <FontImageSource x:Key="ChatSendMessageButtonImageSource"
                     FontFamily="{x:Static telerikMaui:TelerikFont.Name}"
                     Glyph="{x:Static telerikMaui:TelerikFont.IconSendMessage}"
                     Color="{DynamicResource RadOnAppSurfaceColor}"
                     Size="{OnPlatform Default=24, WinUI=22}" />

    <Style x:Key="RadChatStyle" TargetType="telerik:RadChat">
        <Setter Property="Background" Value="{DynamicResource RadSurfaceColor}" />
        <Setter Property="InputAreaBackgroundColor" Value="{DynamicResource RadAppSurfaceColor}" />
        <Setter Property="ItemTemplateSelector" Value="{StaticResource ChatItemTemplateSelector}" />
        <Setter Property="SendMessageButtonImage" Value="{StaticResource ChatSendMessageButtonImageSource}" />
    </Style>
    <Style TargetType="telerik:RadChat" BasedOn="{StaticResource RadChatStyle}" />

</ResourceDictionary>
