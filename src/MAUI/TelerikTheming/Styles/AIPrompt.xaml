<?xml version="1.0" encoding="utf-8" ?>
<ResourceDictionary xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
                    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
                    xmlns:telerik="http://schemas.telerik.com/2022/xaml/maui"
                    xmlns:telerikMaui="clr-namespace:Telerik.Maui;assembly=Telerik.Maui.Core"
                    xmlns:telerikCollectionView="clr-namespace:Telerik.Maui.Controls.CollectionView;assembly=Telerik.Maui.Controls"
                    xmlns:telerikAIPrompt="clr-namespace:Telerik.Maui.Controls.AIPrompt;assembly=Telerik.Maui.Controls"
                    x:Class="Telerik.Theming.Styles.AIPrompt">

    <!-- This is autogenerated file. Use as reference. Do not modify. -->

    <!-- Dependencies: Core.xaml -->

    <Style x:Key="RadAIPromptTabViewHeaderStyle" TargetType="telerik:TabViewHeader">
        <Setter Property="BackgroundColor" Value="{DynamicResource RadSurfaceAltColor}" />
        <Setter Property="BorderColor" Value="{DynamicResource RadBorderColor}" />
        <Setter Property="BorderThickness" Value="0, 0, 0, 1" />
        <Setter Property="CornerRadius" Value="0" />
        <Setter Property="ContentPadding" Value="8" />
        <Setter Property="HorizontalOptions" Value="Fill" />
        <Setter Property="VerticalOptions" Value="Fill" />
    </Style>

    <Style x:Key="RadAIPromptTabViewContentStyle" TargetType="telerik:TabViewContent">
        <Setter Property="BackgroundColor" Value="{DynamicResource RadSurfaceColor}" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="CornerRadius" Value="0" />
        <Setter Property="Padding" Value="0" />
    </Style>

    <Style x:Key="RadAIPromptHeaderItemStyle" TargetType="telerik:TabViewHeaderItem">
        <Setter Property="TextColor" Value="{DynamicResource RadPrimaryColor}" />
        <Setter Property="BorderColor" Value="Transparent" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="CornerRadius" Value="4" />
        <Setter Property="ContentPadding" Value="9, 5" />
        <Setter Property="ImagePosition" Value="Left" />
        <Setter Property="ImageSpacing" Value="2" />
        <Setter Property="ImageWidth" Value="16" />
        <Setter Property="ImageHeight" Value="16" />
        <Setter Property="Margin" Value="0, 0, 8, 0" />
        <Setter Property="HorizontalTextAlignment" Value="Center" />
        <Setter Property="VerticalTextAlignment" Value="Center" />
        <Setter Property="HorizontalOptions" Value="Start" />
        <Setter Property="VerticalOptions" Value="Center" />
        <Setter Property="VisualStateManager.VisualStateGroups">
            <VisualStateGroupList>
                <VisualStateGroup Name="CommonStates">
                    <VisualState Name="Normal">
                        <VisualState.Setters>
                            <Setter Property="telerik:TabViewHeaderItem.BackgroundColor" Value="Transparent" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState Name="PointerOver">
                        <VisualState.Setters>
                            <Setter Property="telerik:TabViewHeaderItem.BackgroundColor" Value="{DynamicResource RadPrimaryColorAlpha4}" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState Name="Selected">
                        <VisualState.Setters>
                            <Setter Property="telerik:TabViewHeaderItem.BackgroundColor" Value="{DynamicResource RadPrimaryColorAlpha16}" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState Name="Disabled">
                        <VisualState.Setters>
                            <Setter Property="telerik:TabViewHeaderItem.BackgroundColor" Value="Transparent" />
                            <Setter Property="Opacity" Value="0.6" />
                        </VisualState.Setters>
                    </VisualState>
                </VisualStateGroup>
            </VisualStateGroupList>
        </Setter>
    </Style>

    <ControlTemplate x:Key="RadAIPromptHeaderItemTemplate">
        <Grid>
            <telerik:RadBorder BackgroundColor="{TemplateBinding BackgroundColor}"
                               BorderColor="{TemplateBinding BorderColor}"
                               BorderThickness="{TemplateBinding BorderThickness}"
                               CornerRadius="{TemplateBinding CornerRadius}"
                               Padding="{TemplateBinding ContentPadding}"
                               Content="{TemplateBinding Content}" />
        </Grid>
    </ControlTemplate>

    <Style x:Key="RadAIPromptTabViewStyle" TargetType="telerik:RadTabView">
        <Setter Property="HeaderStyle" Value="{DynamicResource RadAIPromptTabViewHeaderStyle}" />
        <Setter Property="HeaderItemStyle" Value="{StaticResource RadAIPromptHeaderItemStyle}" />
        <Setter Property="HeaderItemTemplate" Value="{StaticResource RadAIPromptHeaderItemTemplate}" />
        <Setter Property="ContentStyle" Value="{DynamicResource RadAIPromptTabViewContentStyle}" />
        <Setter Property="IsHeaderOverlaid" Value="False" />
        <Setter Property="VisualStateManager.VisualStateGroups">
            <VisualStateGroupList>
                <VisualStateGroup x:Name="CommonStates">
                    <VisualState x:Name="Normal" />
                    <VisualState x:Name="Disabled">
                        <VisualState.Setters>
                            <Setter Property="Opacity" Value="0.6" />
                        </VisualState.Setters>
                    </VisualState>
                </VisualStateGroup>
            </VisualStateGroupList>
        </Setter>
    </Style>

    <Style TargetType="telerikAIPrompt:AIPromptInputView">
        <Setter Property="ControlTemplate">
            <ControlTemplate>
                <Grid x:Name="rootGrid"
                      RowDefinitions="Auto, *, Auto"
                      RowSpacing="{OnPlatform Default=4, MacCatalyst=16, WinUI=8}"
                      Margin="{OnPlatform Default=0, MacCatalyst=16}">
                    <Grid Margin="{OnPlatform Default=16, MacCatalyst=0}">
                        <Editor Style="{TemplateBinding ActualInputEditorStyle}"
                                BackgroundColor="{DynamicResource RadAppSurfaceColor}" />
                        <telerik:RadBorder BorderColor="{DynamicResource RadBaseSubtleColor}"
                                           BorderThickness="{OnPlatform Default=1, WinUI='1, 1, 1, 2'}"
                                           CornerRadius="{OnPlatform Default=4, WinUI=5}" />
                    </Grid>
                    <telerik:RadExpander Grid.Row="1"
                                         Style="{TemplateBinding ActualSuggestionsExpanderStyle}" />
                    <OnPlatform x:TypeArguments="View">
                        <On Platform="WinUI">
                            <telerik:RadBorder Grid.Row="2"
                                               BackgroundColor="{DynamicResource RadSurfaceColor}" />
                        </On>
                    </OnPlatform>
                    <OnPlatform x:TypeArguments="View">
                        <On Platform="Android, iOS, WinUI">
                            <Line Grid.Row="2"
                                  Stroke="{DynamicResource RadBorderColor}"
                                  X2="{Binding Width, Source={x:Reference rootGrid}}"
                                  VerticalOptions="Start" />
                        </On>
                    </OnPlatform>
                    <telerik:RadTemplatedButton Grid.Row="2"
                                                Style="{TemplateBinding ActualInputButtonStyle}" />
                </Grid>
            </ControlTemplate>
        </Setter>
    </Style>

    <Style x:Key="AIPromptInputButtonStyle" TargetType="telerik:RadTemplatedButton" BasedOn="{StaticResource RadPrimaryTemplatedButtonStyle}">
        <Setter Property="HorizontalOptions" Value="Start" />
    </Style>

    <Style x:Key="AIPromptInputEditorStyle" TargetType="Editor">
        <Setter Property="PlaceholderColor" Value="{DynamicResource RadSubtleColor}" />
        <Setter Property="TextColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
        <Setter Property="Margin" Value="0" />
    </Style>

    <Style x:Key="AIPromptSuggestionsExpanderStyle" TargetType="telerik:RadExpander">
        <Setter Property="HeaderStyle">
            <Style TargetType="telerik:ExpanderHeader">
                <Setter Property="IndicatorColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
                <Setter Property="BackgroundColor" Value="Transparent" />
                <Setter Property="BorderColor" Value="{DynamicResource RadBorderColor}" />
            </Style>
        </Setter>
    </Style>

    <Shadow x:Key="AIPromptViewItemShadow"
            Offset="{OnPlatform Default='0, 0', WinUI='0, 2'}"
            Radius="{OnPlatform Default=0, WinUI=4}"
            Brush="{OnPlatform Default=Transparent, WinUI=#1F000000}" />

    <Style x:Key="AIPromptSuggestionLabelStyle" TargetType="Label">
        <Setter Property="TextColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
        <Setter Property="VisualStateManager.VisualStateGroups">
            <Setter.Value>
                <VisualStateGroupList>
                    <VisualStateGroup Name="CommonStates">
                        <VisualState Name="Normal" />
                        <VisualState Name="Disabled">
                            <VisualState.Setters>
                                <Setter Property="Opacity" Value="0.6" />
                            </VisualState.Setters>
                        </VisualState>
                    </VisualStateGroup>
                </VisualStateGroupList>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="AIPromptSuggestionStyle" TargetType="telerikAIPrompt:AIPromptInputSuggestionView">
        <Setter Property="BackgroundColor" Value="{DynamicResource RadSurfaceColor}" />
        <Setter Property="BorderColor" Value="{DynamicResource RadBorderColor}" />
        <Setter Property="Shadow" Value="{StaticResource AIPromptViewItemShadow}" />
        <Setter Property="LabelStyle" Value="{DynamicResource AIPromptSuggestionLabelStyle}" />
        <Setter Property="VisualStateManager.VisualStateGroups">
            <VisualStateGroupList>
                <VisualStateGroup Name="CommonStates">
                    <VisualState Name="Normal">
                        <VisualState.Setters>
                            <Setter Property="telerik:RadBorderContentView.BackgroundColor" Value="{DynamicResource RadSurfaceColor}" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState Name="PointerOver">
                        <VisualState.Setters>
                            <Setter Property="telerik:RadBorderContentView.BackgroundColor" Value="{DynamicResource RadSecondarySubtleHoverColor}" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState Name="Disabled">
                        <VisualState.Setters>
                            <Setter Property="telerik:RadBorderContentView.BackgroundColor" Value="{DynamicResource RadSurfaceColor}" />
                        </VisualState.Setters>
                    </VisualState>
                </VisualStateGroup>
            </VisualStateGroupList>
        </Setter>
    </Style>

    <Style x:Key="AIPromptOutputItemTitleLabelStyle" TargetType="Label">
        <Setter Property="TextColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
    </Style>

    <Style x:Key="AIPromptOutputItemInputTextLabelStyle" TargetType="Label">
        <Setter Property="TextColor" Value="{DynamicResource RadSubtleColor}" />
    </Style>

    <Style x:Key="AIPromptOutputItemResponseTextLabelStyle" TargetType="Label">
        <Setter Property="TextColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
    </Style>

    <Style x:Key="AIPromptOutputItemCopyButtonStyle" TargetType="telerik:RadTemplatedButton" BasedOn="{StaticResource RadFlatTemplatedButtonStyle}" />

    <Style x:Key="AIPromptOutputItemRetryButtonStyle" TargetType="telerik:RadTemplatedButton" BasedOn="{StaticResource RadFlatTemplatedButtonStyle}" />

    <ControlTemplate x:Key="AIPromptRatingViewControlTemplate">
        <HorizontalStackLayout Spacing="{OnPlatform iOS=8}">
            <HorizontalStackLayout.Resources>
                <Style TargetType="telerik:RadToggleButton">
                    <Setter Property="MinimumWidthRequest" Value="{OnPlatform Default=36, WinUI=32}" />
                    <Setter Property="MinimumHeightRequest" Value="{OnPlatform Default=36, MacCatalyst=20, WinUI=32}" />
                    <Setter Property="FontFamily" Value="{x:Static telerikMaui:TelerikFont.Name}" />
                    <Setter Property="FontSize" Value="{OnPlatform Default=16, MacCatalyst=12}" />
                    <Setter Property="TextColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
                    <Setter Property="Background" Value="Transparent" />
                    <Setter Property="BorderThickness" Value="0" />
                    <Setter Property="Padding" Value="0" />
                    <Setter Property="Shadow" Value="{x:Null}" />
                    <Setter Property="VerticalOptions" Value="Center" />
                    <Setter Property="VisualStateManager.VisualStateGroups">
                        <VisualStateGroupList>
                            <VisualStateGroup Name="CommonStates">
                                <VisualState Name="Normal">
                                    <VisualState.Setters>
                                        <Setter Property="telerik:RadToggleButton.TextColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
                                        <Setter Property="telerik:RadToggleButton.Background" Value="Transparent" />
                                    </VisualState.Setters>
                                </VisualState>
                                <VisualState Name="MouseOver">
                                    <VisualState.Setters>
                                        <Setter Property="telerik:RadToggleButton.TextColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
                                        <Setter Property="telerik:RadToggleButton.Background" Value="{DynamicResource RadOnAppSurfaceColorAlpha4}" />
                                    </VisualState.Setters>
                                </VisualState>
                                <VisualState Name="Pressed">
                                    <VisualState.Setters>
                                        <Setter Property="telerik:RadToggleButton.TextColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
                                        <Setter Property="telerik:RadToggleButton.Background" Value="{DynamicResource RadOnAppSurfaceColorAlpha16}" />
                                    </VisualState.Setters>
                                </VisualState>
                                <VisualState Name="Disabled">
                                    <VisualState.Setters>
                                        <Setter Property="telerik:RadToggleButton.TextColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
                                        <Setter Property="telerik:RadToggleButton.Background" Value="Transparent" />
                                        <Setter Property="Opacity" Value="0.6" />
                                    </VisualState.Setters>
                                </VisualState>
                                <VisualState Name="Toggled">
                                    <VisualState.Setters>
                                        <Setter Property="telerik:RadToggleButton.TextColor" Value="{DynamicResource RadPrimaryColor}" />
                                        <Setter Property="telerik:RadToggleButton.Background" Value="Transparent" />
                                    </VisualState.Setters>
                                </VisualState>
                                <VisualState Name="ToggledMouseOver">
                                    <VisualState.Setters>
                                        <Setter Property="telerik:RadToggleButton.TextColor" Value="{DynamicResource RadPrimaryColor}" />
                                        <Setter Property="telerik:RadToggleButton.Background" Value="{DynamicResource RadOnAppSurfaceColorAlpha4}" />
                                    </VisualState.Setters>
                                </VisualState>
                                <VisualState Name="ToggledPressed">
                                    <VisualState.Setters>
                                        <Setter Property="telerik:RadToggleButton.TextColor" Value="{DynamicResource RadPrimaryColor}" />
                                        <Setter Property="telerik:RadToggleButton.Background" Value="{DynamicResource RadOnAppSurfaceColorAlpha16}" />
                                    </VisualState.Setters>
                                </VisualState>
                                <VisualState Name="ToggledDisabled">
                                    <VisualState.Setters>
                                        <Setter Property="telerik:RadToggleButton.TextColor" Value="{DynamicResource RadPrimaryColor}" />
                                        <Setter Property="telerik:RadToggleButton.Background" Value="Transparent" />
                                        <Setter Property="Opacity" Value="0.6" />
                                    </VisualState.Setters>
                                </VisualState>
                            </VisualStateGroup>
                        </VisualStateGroupList>
                    </Setter>
                </Style>
            </HorizontalStackLayout.Resources>
            <telerik:RadToggleButton x:Name="PART_LikeToggleButton"
                                     Content="{x:Static telerikMaui:TelerikFont.IconThumbsUp}" />
            <telerik:RadToggleButton x:Name="PART_DislikeToggleButton"
                                     Content="{x:Static telerikMaui:TelerikFont.IconThumbsDown}" />
        </HorizontalStackLayout>
    </ControlTemplate>

    <Style x:Key="AIPromptRatingStyle" TargetType="telerikAIPrompt:AIPromptRatingView">
        <Setter Property="ControlTemplate" Value="{StaticResource AIPromptRatingViewControlTemplate}" />
    </Style>

    <Style x:Key="AIPromptOutputItemStyle" TargetType="telerikAIPrompt:AIPromptOutputItemView">
        <Setter Property="BackgroundColor" Value="{DynamicResource RadSurfaceAltColor}" />
        <Setter Property="BorderColor" Value="{DynamicResource RadBorderColor}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="CornerRadius" Value="{OnPlatform Default=8, Android=4, iOS=12}" />
        <Setter Property="Margin" Value="{OnPlatform Default=0, WinUI='16, 0'}" />
        <Setter Property="TitleLabelStyle" Value="{DynamicResource AIPromptOutputItemTitleLabelStyle}" />
        <Setter Property="InputTextLabelStyle" Value="{DynamicResource AIPromptOutputItemInputTextLabelStyle}" />
        <Setter Property="ResponseTextLabelStyle" Value="{DynamicResource AIPromptOutputItemResponseTextLabelStyle}" />
        <Setter Property="CopyButtonStyle" Value="{DynamicResource AIPromptOutputItemCopyButtonStyle}" />
        <Setter Property="RetryButtonStyle" Value="{DynamicResource AIPromptOutputItemRetryButtonStyle}" />
        <Setter Property="RatingStyle" Value="{DynamicResource AIPromptRatingStyle}" />
    </Style>

    <DataTemplate x:Key="AIPromptOutputItemTemplate">
        <telerikAIPrompt:AIPromptOutputItemView Style="{Binding ActualOutputItemStyle, Source={RelativeSource AncestorType={x:Type telerikAIPrompt:AIPromptOutputView}}}"
                                                Shadow="{StaticResource AIPromptViewItemShadow}" />
    </DataTemplate>

    <Style x:Key="AIPromptOuputViewItemStyle" TargetType="telerik:RadCollectionViewItemView">
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="ControlTemplate">
            <ControlTemplate>
                <ContentPresenter />
            </ControlTemplate>
        </Setter>
        <Setter Property="VisualStateManager.VisualStateGroups">
            <VisualStateGroupList>
                <VisualStateGroup Name="CommonStates">
                    <VisualState Name="Normal" />
                    <VisualState Name="MouseOver" />
                    <VisualState Name="Selected" />
                    <VisualState Name="Disabled" />
                </VisualStateGroup>
                <VisualStateGroup Name="OrientationStates">
                    <VisualState Name="Vertical" />
                    <VisualState Name="Horizontal" />
                </VisualStateGroup>
            </VisualStateGroupList>
        </Setter>
    </Style>

    <Style TargetType="telerikAIPrompt:AIPromptOutputView">
        <Setter Property="ControlTemplate">
            <ControlTemplate>
                <telerik:RadCollectionView ItemTemplate="{StaticResource AIPromptOutputItemTemplate}"
                                           ItemsSource="{TemplateBinding OutputItems}"
                                           ItemViewStyle="{DynamicResource AIPromptOuputViewItemStyle}"
                                           SelectionMode="None"
                                           Margin="{OnPlatform Default=16, WinUI='0, 16'}">
                    <telerik:RadCollectionView.ItemsLayout>
                        <telerikCollectionView:CollectionViewLinearLayout Orientation="Vertical"
                                                                          ItemSpacing="{OnPlatform Default=4, MacCatalyst=12, WinUI=16}" />
                    </telerik:RadCollectionView.ItemsLayout>
                </telerik:RadCollectionView>
            </ControlTemplate>
        </Setter>
    </Style>

    <DataTemplate x:Key="AIPromptCommandTemplate">
        <telerikAIPrompt:AIPromptCommandItemView Style="{Binding ActualCommandStyle, Source={RelativeSource AncestorType={x:Type telerikAIPrompt:AIPromptCommandView}}}" />
    </DataTemplate>

    <DataTemplate x:Key="AIPromptCommandGroupTemplate">
        <telerikAIPrompt:AIPromptCommandGroupView Style="{Binding ActualCommandGroupStyle, Source={RelativeSource AncestorType={x:Type telerikAIPrompt:AIPromptCommandView}}}"  />
    </DataTemplate>

    <telerikAIPrompt:AIPromptCommandTemplateSelector x:Key="AIPromptCommandTemplateSelector"
                                                     CommandTemplate="{StaticResource AIPromptCommandTemplate}"
                                                     GroupTemplate="{StaticResource AIPromptCommandGroupTemplate}" />

    <DataTemplate x:Key="AIPromptCommandsItemsControlItemsLayoutTemplate">
        <VerticalStackLayout Spacing="{OnPlatform Default=4, MacCatalyst=8, WinUI=0}" />
    </DataTemplate>

    <Style TargetType="telerikAIPrompt:AIPromptCommandView">
        <Setter Property="ControlTemplate">
            <ControlTemplate x:Key="AIPromptCommandView_ControlTemplate">
                <telerik:NonVirtualizedItemsControl SelectionMode="None"
                                                    ItemTemplate="{StaticResource AIPromptCommandTemplateSelector}"
                                                    ItemTappedCommand="{TemplateBinding CommandTappedCommand}"
                                                    ItemsSource="{TemplateBinding Commands}"
                                                    ItemsLayoutTemplate="{StaticResource AIPromptCommandsItemsControlItemsLayoutTemplate}"
                                                    Margin="{OnPlatform Default=16, WinUI='0, 16'}" />
            </ControlTemplate>
        </Setter>
    </Style>

    <Style x:Key="AIPromptCommandLabelStyle" TargetType="Label">
        <Setter Property="TextColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
        <Setter Property="VisualStateManager.VisualStateGroups">
            <Setter.Value>
                <VisualStateGroupList>
                    <VisualStateGroup Name="CommonStates">
                        <VisualState Name="Normal" />
                        <VisualState Name="Disabled">
                            <VisualState.Setters>
                                <Setter Property="Opacity" Value="0.6" />
                            </VisualState.Setters>
                        </VisualState>
                    </VisualStateGroup>
                </VisualStateGroupList>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="AIPromptCommandStyle" TargetType="telerikAIPrompt:AIPromptCommandItemView">
        <Setter Property="BackgroundColor" Value="{DynamicResource RadSurfaceColor}" />
        <Setter Property="BorderColor" Value="{DynamicResource RadBorderColor}" />
        <Setter Property="Shadow" Value="{StaticResource AIPromptViewItemShadow}" />
        <Setter Property="LabelStyle" Value="{DynamicResource AIPromptCommandLabelStyle}" />
        <Setter Property="VisualStateManager.VisualStateGroups">
            <VisualStateGroupList>
                <VisualStateGroup Name="CommonStates">
                    <VisualState Name="Normal">
                        <VisualState.Setters>
                            <Setter Property="telerik:RadBorderContentView.BackgroundColor" Value="{DynamicResource RadSurfaceColor}" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState Name="PointerOver">
                        <VisualState.Setters>
                            <Setter Property="telerik:RadBorderContentView.BackgroundColor" Value="{DynamicResource RadSecondarySubtleHoverColor}" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState Name="Disabled">
                        <VisualState.Setters>
                            <Setter Property="telerik:RadBorderContentView.BackgroundColor" Value="{DynamicResource RadSurfaceColor}" />
                        </VisualState.Setters>
                    </VisualState>
                </VisualStateGroup>
            </VisualStateGroupList>
        </Setter>
    </Style>

    <Style x:Key="AIPromptCommandGroupStyle" TargetType="telerikAIPrompt:AIPromptCommandGroupView">
        <Setter Property="ExpanderStyle">
            <Style TargetType="telerik:RadExpander">
                <Setter Property="HeaderStyle">
                    <Style TargetType="telerik:ExpanderHeader">
                        <Setter Property="IndicatorColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
                        <Setter Property="BackgroundColor" Value="Transparent" />
                        <Setter Property="BorderColor" Value="{DynamicResource RadBorderColor}" />
                    </Style>
                </Setter>
            </Style>
        </Setter>
    </Style>

    <Style x:Key="RadAIPromptStyle" TargetType="telerik:RadAIPrompt">
        <Setter Property="TabViewStyle" Value="{StaticResource RadAIPromptTabViewStyle}" />
        <Setter Property="InputButtonStyle" Value="{StaticResource AIPromptInputButtonStyle}" />
        <Setter Property="InputEditorStyle" Value="{StaticResource AIPromptInputEditorStyle}" />
        <Setter Property="SuggestionsExpanderStyle" Value="{StaticResource AIPromptSuggestionsExpanderStyle}" />
        <Setter Property="SuggestionStyle" Value="{StaticResource AIPromptSuggestionStyle}" />
        <Setter Property="OutputItemStyle" Value="{StaticResource AIPromptOutputItemStyle}" />
        <Setter Property="CommandStyle" Value="{DynamicResource AIPromptCommandStyle}" />
        <Setter Property="CommandGroupStyle" Value="{DynamicResource AIPromptCommandGroupStyle}" />
    </Style>
    <Style TargetType="telerik:RadAIPrompt" BasedOn="{StaticResource RadAIPromptStyle}" />

    <Style x:Key="RadAIPromptClosePopupButtonStyle" TargetType="telerik:RadTemplatedButton" BasedOn="{StaticResource RadTemplatedButtonStyle}">
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Padding" Value="0" />
    </Style>

    <Shadow x:Key="AIPromptPopupContentViewShadow"
            Offset="{OnPlatform Default='0, 0', MacCatalyst='0, 8', WinUI='0, 8'}"
            Radius="{OnPlatform Default=0, MacCatalyst=40, WinUI=16}"
            Brush="{OnPlatform Default=Transparent, MacCatalyst=#40000000, WinUI=#24000000}" />

    <ControlTemplate x:Key="AIPromptPopupContentViewControlTemplate">
        <telerik:RadBorder BackgroundColor="{TemplateBinding BackgroundColor}"
                           Background="{TemplateBinding Background}"
                           BorderColor="{TemplateBinding BorderColor}"
                           BorderBrush="{TemplateBinding BorderBrush}"
                           BorderThickness="{TemplateBinding BorderThickness}"
                           CornerRadius="{TemplateBinding CornerRadius}"
                           Padding="{TemplateBinding ContentPadding}"
                           Shadow="{StaticResource AIPromptPopupContentViewShadow}">
            <Grid RowDefinitions="48, *">
                <ContentPresenter Grid.RowSpan="2" />
                <Grid Padding="8"
                      Margin="8"
                      HorizontalOptions="End"
                      VerticalOptions="Center">
                    <View.GestureRecognizers>
                        <TapGestureRecognizer Command="{TemplateBinding ActualClosePopupCommand}" />
                    </View.GestureRecognizers>
                    <telerik:RadTemplatedButton Style="{TemplateBinding ActualClosePopupButtonStyle}" />
                </Grid>
            </Grid>
        </telerik:RadBorder>
    </ControlTemplate>

    <Style x:Key="RadAIPromptPopupContentViewStyle" TargetType="telerikAIPrompt:AIPromptPopupContentView">
        <Setter Property="ControlTemplate" Value="{StaticResource AIPromptPopupContentViewControlTemplate}" />
        <Setter Property="BackgroundColor" Value="{DynamicResource RadSurfaceAltColor}" />
        <Setter Property="ClosePopupButtonStyle" Value="{StaticResource RadAIPromptClosePopupButtonStyle}" />
        <Setter Property="CornerRadius" Value="0" />
    </Style>

    <Style x:Key="RadAIPromptButtonStyle" TargetType="telerik:RadAIPromptButton">
        <Setter Property="WidthRequest" Value="36" />
        <Setter Property="HeightRequest" Value="36" />
        <Setter Property="FontFamily" Value="{x:Static telerikMaui:TelerikFont.Name}" />
        <Setter Property="FontSize" Value="16" />
        <Setter Property="TextColor" Value="{DynamicResource RadOnPrimaryColor}" />
        <Setter Property="Background" Value="{DynamicResource RadPrimaryColor}" />
        <Setter Property="BorderBrush" Value="Transparent" />
        <Setter Property="Content" Value="{x:Static telerikMaui:TelerikFont.IconSparkle}" />
        <Setter Property="CornerRadius" Value="18" />
        <Setter Property="Padding" Value="0" />
        <Setter Property="HorizontalTextAlignment" Value="Center" />
        <Setter Property="VerticalTextAlignment" Value="Center" />
        <Setter Property="PopupContentViewStyle" Value="{StaticResource RadAIPromptPopupContentViewStyle}" />
        <Setter Property="VisualStateManager.VisualStateGroups">
            <VisualStateGroupList>
                <VisualStateGroup x:Name="CommonStates">
                    <VisualState x:Name="Normal">
                        <VisualState.Setters>
                            <Setter Property="Background" Value="{DynamicResource RadPrimaryColor}" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState x:Name="PointerOver">
                        <VisualState.Setters>
                            <Setter Property="Background" Value="{DynamicResource RadPrimaryHoverColor}" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState x:Name="Pressed">
                        <VisualState.Setters>
                            <Setter Property="Background" Value="{DynamicResource RadPrimaryActiveColor}" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState x:Name="Disabled">
                        <VisualState.Setters>
                            <Setter Property="Background" Value="{DynamicResource RadPrimaryColor}" />
                            <Setter Property="Opacity" Value="0.6" />
                        </VisualState.Setters>
                    </VisualState>
                </VisualStateGroup>
            </VisualStateGroupList>
        </Setter>
    </Style>
    <Style TargetType="telerik:RadAIPromptButton" BasedOn="{StaticResource RadAIPromptButtonStyle}" />

</ResourceDictionary>