<?xml version="1.0" encoding="utf-8" ?>
<ResourceDictionary xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
                    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
                    xmlns:telerik="http://schemas.telerik.com/2022/xaml/maui"
                    xmlns:telerikMaui="clr-namespace:Telerik.Maui;assembly=Telerik.Maui.Core"
                    xmlns:telerikAutoComplete="clr-namespace:Telerik.Maui.Controls.AutoComplete;assembly=Telerik.Maui.Controls"
                    xmlns:telerikImageEditor="clr-namespace:Telerik.Maui.Controls.ImageEditor;assembly=Telerik.Maui.Controls"
                    xmlns:telerikCollectionView="clr-namespace:Telerik.Maui.Controls.CollectionView;assembly=Telerik.Maui.Controls"
                    xmlns:telerikAIPrompt="clr-namespace:Telerik.Maui.Controls.AIPrompt;assembly=Telerik.Maui.Controls"
                    x:Class="Telerik.Theming.Styles.CollectionView">

    <!-- This is autogenerated file. Use as reference. Do not modify. -->

    <DataTemplate x:Key="RadCollectionViewItemTemplate">
        <Label telerik:LabelUtils.TextPath="{Binding DisplayMemberPath, Source={RelativeSource AncestorType={x:Type telerik:RadCollectionView}}}"
               Padding="{Binding Source={RelativeSource AncestorType={x:Type telerik:RadCollectionViewItemView}}, Path=ActualPadding}"
               HorizontalTextAlignment="Start"
               VerticalTextAlignment="Center"
               AutomationId="RadCollectionViewItemLabel">
            <Label.Triggers>
                <DataTrigger TargetType="Label" Binding="{Binding IsSelected, Source={RelativeSource AncestorType={x:Type telerik:RadCollectionViewItemView}}}" Value="True">
                    <Setter Property="TextColor" Value="{DynamicResource RadOnPrimaryColor}" />
                </DataTrigger>
                <DataTrigger TargetType="Label" Binding="{Binding IsSelected, Source={RelativeSource AncestorType={x:Type telerik:RadCollectionViewItemView}}}" Value="False">
                    <Setter Property="TextColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
                </DataTrigger>
                <MultiTrigger TargetType="Label">
                    <MultiTrigger.Conditions>
                        <BindingCondition Binding="{Binding IsSelected, Source={RelativeSource AncestorType={x:Type telerik:RadCollectionViewItemView}}}" Value="True" />
                        <BindingCondition Binding="{Binding IsMouseOver, Source={RelativeSource AncestorType={x:Type telerik:RadCollectionViewItemView}}}" Value="True" />
                    </MultiTrigger.Conditions>
                    <Setter Property="TextColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
                </MultiTrigger>
            </Label.Triggers>
        </Label>
    </DataTemplate>

    <Style x:Key="RadCollectionViewItemStyle" TargetType="telerik:RadCollectionViewItemView">
        <Setter Property="ControlTemplate">
            <ControlTemplate>
                <ContentPresenter />
            </ControlTemplate>
        </Setter>
        <Setter Property="BackgroundColor" Value="Transparent" />
        <Setter Property="BorderColor" Value="{DynamicResource RadBorderColor}" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="CornerRadius" Value="0" />
        <Setter Property="VisualStateManager.VisualStateGroups">
            <VisualStateGroupList>
                <VisualStateGroup Name="CommonStates">
                    <VisualState Name="Normal">
                        <VisualState.Setters>
                            <Setter Property="telerik:RadCollectionViewItemView.BackgroundColor" Value="Transparent" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState Name="MouseOver">
                        <VisualState.Setters>
                            <Setter Property="telerik:RadCollectionViewItemView.BackgroundColor" Value="{DynamicResource RadBorderColor}" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState Name="Selected">
                        <VisualState.Setters>
                            <Setter Property="telerik:RadCollectionViewItemView.BackgroundColor" Value="{DynamicResource RadPrimaryColor}" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState Name="Disabled">
                        <VisualState.Setters>
                            <Setter Property="telerik:RadCollectionViewItemView.BackgroundColor" Value="Transparent" />
                            <Setter Property="Opacity" Value="0.6" />
                        </VisualState.Setters>
                    </VisualState>
                </VisualStateGroup>
            </VisualStateGroupList>
        </Setter>
    </Style>

    <Style x:Key="RadCollectionViewGroupItemExpandCollapseIndicatorStyle" TargetType="Label">
        <Setter Property="TextColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
        <Setter Property="VisualStateManager.VisualStateGroups">
            <VisualStateGroupList>
                <VisualStateGroup x:Name="CommonStates">
                    <VisualState x:Name="Normal"/>
                    <VisualState x:Name="Disabled">
                        <VisualState.Setters>
                            <Setter Property="Opacity" Value="0.6" />
                        </VisualState.Setters>
                    </VisualState>
                </VisualStateGroup>
            </VisualStateGroupList>
        </Setter>
    </Style>

    <DataTemplate x:Key="RadCollectionViewGroupHeaderTemplate">
        <Label Text="{Binding Key}"
               FontSize="{OnPlatform Default=14, Android=16, iOS=17}"
               FontAttributes="{OnPlatform Default=Bold, WinUI=None}"
               TextColor="{DynamicResource RadOnAppSurfaceColor}"
               HorizontalTextAlignment="Start"
               VerticalTextAlignment="Center"
               AutomationId="RadCollectionViewGroupHedaerLabel">
            <VisualStateManager.VisualStateGroups>
                <VisualStateGroupList>
                    <VisualStateGroup Name="CommonStates">
                        <VisualState Name="Normal" />
                        <VisualState Name="Disabled">
                            <VisualState.Setters>
                                <Setter Property="Opacity" Value="0.6" />
                            </VisualState.Setters>
                        </VisualState>
                    </VisualStateGroup>
                </VisualStateGroupList>
            </VisualStateManager.VisualStateGroups>
        </Label>
    </DataTemplate>

    <Style x:Key="RadCollectionViewGroupItemStyle" TargetType="telerik:RadCollectionViewGroupView">
        <Setter Property="BackgroundColor" Value="{DynamicResource RadSurfaceAltColor}" />
        <Setter Property="BorderColor" Value="{DynamicResource RadBorderColor}" />
        <Setter Property="BorderThickness" Value="0, 0, 0, 1" />
        <Setter Property="ExpandCollapseIndicatorStyle" Value="{DynamicResource RadCollectionViewGroupItemExpandCollapseIndicatorStyle}" />
        <Setter Property="VisualStateManager.VisualStateGroups">
            <VisualStateGroupList>
                <VisualStateGroup Name="CommonStates">
                    <VisualState Name="Normal" />
                    <VisualState Name="Disabled" />
                </VisualStateGroup>
                <VisualStateGroup Name="OrientationStates">
                    <VisualState Name="Vertical" />
                    <VisualState Name="Horizontal">
                        <VisualState.Setters>
                            <Setter Property="telerik:RadCollectionViewItemView.BorderThickness" Value="0, 0, 1, 0" />
                        </VisualState.Setters>
                    </VisualState>
                </VisualStateGroup>
            </VisualStateGroupList>
        </Setter>
    </Style>

    <DataTemplate x:Key="RadCollectionViewDropIndicatorTemplate">
        <telerikCollectionView:CollectionViewDropIndicator StrokeColor="{DynamicResource RadPrimaryColor}" />
    </DataTemplate>

    <telerik:InvertedBooleanConverter x:Key="InvertedBooleanConverter" />

    <DataTemplate x:Key="RadCollectionViewManualLoadOnDemandTemplate">
        <Grid HorizontalOptions="Center"
              ColumnDefinitions="Auto, *"
              ColumnSpacing="8"
              HeightRequest="35"
              Padding="0, 5"
              IsEnabled="{Binding Source={RelativeSource AncestorType={x:Type telerik:RadCollectionView}}, Path=IsLoadOnDemandActive, Converter={StaticResource InvertedBooleanConverter}}">
            <telerik:RadBusyIndicator AnimationType="Animation9"
                                      AnimationContentColor="{DynamicResource RadPrimaryColor}"
                                      VerticalOptions="Center"
                                      IsBusy="True"
                                      IsVisible="{Binding Source={RelativeSource AncestorType={x:Type telerik:RadCollectionView}}, Path=IsLoadOnDemandActive}" />
            <Label Grid.Column="1"
                   Text="{telerik:Localize CollectionView_ManualLoadOnDemandText}"
                   TextColor="{DynamicResource RadPrimaryColor}"
                   VerticalOptions="Center" />
        </Grid>
    </DataTemplate>

    <DataTemplate x:Key="RadCollectionViewAutomaticLoadOnDemandTemplate">
        <telerik:RadBusyIndicator WidthRequest="25"
                                  HeightRequest="25"
                                  AnimationType="Animation9"
                                  AnimationContentColor="{DynamicResource RadPrimaryColor}"
                                  IsBusy="{Binding Source={RelativeSource AncestorType={x:Type telerik:RadCollectionView}}, Path=IsLoadOnDemandActive}"
                                  Margin="0, 5"
                                  HorizontalOptions="Center"
                                  VerticalOptions="Center" />
    </DataTemplate>

    <Style x:Key="RadCollectionViewStyle" TargetType="telerik:RadCollectionView">
        <Setter Property="ItemTemplate" Value="{StaticResource RadCollectionViewItemTemplate}" />
        <Setter Property="ItemViewStyle" Value="{StaticResource RadCollectionViewItemStyle}" />
        <Setter Property="GroupHeaderTemplate" Value="{StaticResource RadCollectionViewGroupHeaderTemplate}" />
        <Setter Property="GroupViewStyle" Value="{StaticResource RadCollectionViewGroupItemStyle}" />
        <Setter Property="DropIndicatorTemplate" Value="{StaticResource RadCollectionViewDropIndicatorTemplate}" />
        <Setter Property="ManualLoadOnDemandTemplate" Value="{StaticResource RadCollectionViewManualLoadOnDemandTemplate}" />
        <Setter Property="AutomaticLoadOnDemandTemplate" Value="{StaticResource RadCollectionViewAutomaticLoadOnDemandTemplate}" />
    </Style>
    <Style TargetType="telerik:RadCollectionView" BasedOn="{StaticResource RadCollectionViewStyle}" />

</ResourceDictionary>
