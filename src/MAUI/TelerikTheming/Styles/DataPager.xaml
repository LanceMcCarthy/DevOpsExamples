<?xml version="1.0" encoding="utf-8" ?>
<ResourceDictionary xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
                    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
                    xmlns:telerik="http://schemas.telerik.com/2022/xaml/maui"
                    xmlns:telerikDataPager="clr-namespace:Telerik.Maui.Controls.DataPager;assembly=Telerik.Maui.Controls"
                    x:Class="Telerik.Theming.Styles.DataPager">

    <!-- This is autogenerated file. Use as reference. Do not modify. -->

    <ControlTemplate x:Key="DataPagerButtonControlTemplate">
        <telerik:RadEffectsView x:Name="PART_EffectsView"
                                BackgroundColor="{TemplateBinding BackgroundColor}"
                                Background="{TemplateBinding Background}"
                                BorderColor="{TemplateBinding BorderColor}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="{TemplateBinding CornerRadius, Converter={x:Static telerik:CornerRadiusToThicknessConverter.Instance}}"
                                Padding="{TemplateBinding Padding}">
            <ContentPresenter telerik:ContentPresenterUtils.Content="{TemplateBinding ActualContent}" />
        </telerik:RadEffectsView>
    </ControlTemplate>

    <Style x:Key="DataPagerButtonStyle" TargetType="telerikDataPager:DataPagerButton">
        <Setter Property="ControlTemplate" Value="{StaticResource DataPagerButtonControlTemplate}" />
        <Setter Property="TextColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderBrush" Value="Transparent" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Padding" Value="5, 0" />
        <Setter Property="MinimumWidthRequest" Value="30" />
        <Setter Property="MinimumHeightRequest" Value="30" />
        <Setter Property="CornerRadius" Value="4" />
        <Setter Property="Shadow" Value="{x:Null}" />
        <Setter Property="ToolTipProperties.Text" Value="{Binding CommandParameter, Source={RelativeSource Mode=Self}, Converter={Static telerik:ArithmeticConverter.Plus1Converter}}" />
        <Setter Property="VisualStateManager.VisualStateGroups">
            <VisualStateGroupList>
                <VisualStateGroup Name="CommonStates">
                    <VisualState Name="Normal">
                        <VisualState.Setters>
                            <Setter Property="telerikDataPager:DataPagerButton.TextColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
                            <Setter Property="telerikDataPager:DataPagerButton.Background" Value="Transparent" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState Name="PointerOver">
                        <VisualState.Setters>
                            <Setter Property="telerikDataPager:DataPagerButton.TextColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
                            <Setter Property="telerikDataPager:DataPagerButton.Background" Value="{DynamicResource RadOnAppSurfaceColorAlpha4}" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState Name="Pressed">
                        <VisualState.Setters>
                            <Setter Property="telerikDataPager:DataPagerButton.TextColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
                            <Setter Property="telerikDataPager:DataPagerButton.Background" Value="{DynamicResource RadOnAppSurfaceColorAlpha16}" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState Name="Selected">
                        <VisualState.Setters>
                            <Setter Property="telerikDataPager:DataPagerButton.TextColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
                            <Setter Property="telerikDataPager:DataPagerButton.Background" Value="Transparent" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState Name="SelectedDisabled">
                        <VisualState.Setters>
                            <Setter Property="telerikDataPager:DataPagerButton.TextColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
                            <Setter Property="telerikDataPager:DataPagerButton.Background" Value="Transparent" />
                            <Setter Property="ToolTipProperties.Text" Value="{Null}" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState Name="Disabled">
                        <VisualState.Setters>
                            <Setter Property="telerikDataPager:DataPagerButton.TextColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
                            <Setter Property="telerikDataPager:DataPagerButton.Background" Value="Transparent" />
                            <Setter Property="telerikDataPager:DataPagerButton.Opacity" Value="0.6" />
                            <Setter Property="ToolTipProperties.Text" Value="{Null}" />
                        </VisualState.Setters>
                    </VisualState>
                </VisualStateGroup>
            </VisualStateGroupList>
        </Setter>
    </Style>

    <Style x:Key="DataPagerNumericButtonStyle" TargetType="telerikDataPager:DataPagerButton" BasedOn="{StaticResource DataPagerButtonStyle}">
        <Setter Property="TextColor" Value="{DynamicResource RadPrimaryColor}" />
        <Setter Property="VisualStateManager.VisualStateGroups">
            <VisualStateGroupList>
                <VisualStateGroup Name="CommonStates">
                    <VisualState Name="Normal">
                        <VisualState.Setters>
                            <Setter Property="telerikDataPager:DataPagerButton.Background" Value="Transparent" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState Name="PointerOver">
                        <VisualState.Setters>
                            <Setter Property="telerikDataPager:DataPagerButton.Background" Value="{DynamicResource RadPrimaryColorAlpha4}" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState Name="Pressed">
                        <VisualState.Setters>
                            <Setter Property="telerikDataPager:DataPagerButton.Background" Value="{DynamicResource RadPrimaryColorAlpha16}" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState Name="Selected">
                        <VisualState.Setters>
                            <Setter Property="telerikDataPager:DataPagerButton.Background" Value="{DynamicResource RadPrimaryColorAlpha16}" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState Name="SelectedDisabled">
                        <VisualState.Setters>
                            <Setter Property="telerikDataPager:DataPagerButton.Background" Value="{DynamicResource RadPrimaryColorAlpha16}" />
                            <Setter Property="ToolTipProperties.Text" Value="{Null}" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState Name="Disabled">
                        <VisualState.Setters>
                            <Setter Property="telerikDataPager:DataPagerButton.Background" Value="Transparent" />
                            <Setter Property="telerikDataPager:DataPagerButton.Opacity" Value="0.6" />
                            <Setter Property="ToolTipProperties.Text" Value="{Null}" />
                        </VisualState.Setters>
                    </VisualState>
                </VisualStateGroup>
            </VisualStateGroupList>
        </Setter>
    </Style>

    <Style x:Key="DataPagerLabelStyle" TargetType="Label">
        <Setter Property="TextColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
        <Setter Property="VisualStateManager.VisualStateGroups">
            <VisualStateGroupList>
                <VisualStateGroup Name="CommonStates">
                    <VisualState Name="Normal" />
                    <VisualState Name="Disabled" />
                </VisualStateGroup>
            </VisualStateGroupList>
        </Setter>
    </Style>

    <Style x:Key="DataPagerComboBoxStyle" TargetType="telerik:RadComboBox">
        <Setter Property="BackgroundColor" Value="{DynamicResource RadAppSurfaceColor}" />
        <Setter Property="TextInputStyle">
            <Style TargetType="telerik:RadTextInput">
                <Setter Property="MinimumHeightRequest" Value="{OnPlatform Default=0, iOS=19}" />
                <Setter Property="Padding" Value="{OnPlatform Android='9, 7', iOS='9, 6.5', MacCatalyst='9, 5.75', WinUI='9, 4.5'}" />
            </Style>
        </Setter>
    </Style>
    
    <Style x:Key="DataPagerPageSizesViewStyle" TargetType="telerikDataPager:DataPagerPageSizesView">
        <Setter Property="LabelStyle" Value="{DynamicResource DataPagerLabelStyle}" />
        <Setter Property="ComboBoxStyle" Value="{DynamicResource DataPagerComboBoxStyle}" />
    </Style>

    <Style x:Key="DataPagerNavigationViewStyle" TargetType="telerikDataPager:DataPagerNavigationView">
        <Setter Property="LabelsStyle" Value="{DynamicResource DataPagerLabelStyle}" />
        <Setter Property="ComboBoxStyle" Value="{DynamicResource DataPagerComboBoxStyle}" />
    </Style>

    <Style x:Key="RadDataPagerStyle" TargetType="telerik:RadDataPager">
        <Setter Property="Background" Value="{DynamicResource RadSurfaceColor}" />
        <Setter Property="BorderBrush" Value="{DynamicResource RadBorderColor}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="ContentPadding" Value="8" />
        <Setter Property="HeightRequest" Value="48" />
        <Setter Property="NavigateToFirstPageButtonStyle" Value="{DynamicResource DataPagerButtonStyle}" />
        <Setter Property="NavigateToPreviousPageButtonStyle" Value="{DynamicResource DataPagerButtonStyle}" />
        <Setter Property="NavigateToNextPageButtonStyle" Value="{DynamicResource DataPagerButtonStyle}" />
        <Setter Property="NavigateToLastPageButtonStyle" Value="{DynamicResource DataPagerButtonStyle}" />
        <Setter Property="NumericButtonStyle" Value="{DynamicResource DataPagerNumericButtonStyle}" />
        <Setter Property="NavigationComboBoxStyle" Value="{DynamicResource DataPagerComboBoxStyle}" />
        <Setter Property="PageSizesViewStyle" Value="{DynamicResource DataPagerPageSizesViewStyle}" />
        <Setter Property="NavigationViewStyle" Value="{DynamicResource DataPagerNavigationViewStyle}" />
        <Setter Property="VisualStateManager.VisualStateGroups">
            <VisualStateGroupList>
                <VisualStateGroup Name="CommonStates">
                    <VisualState Name="Normal" />
                    <VisualState Name="Disabled">
                        <VisualState.Setters>
                            <Setter Property="Opacity" Value="0.6" />
                        </VisualState.Setters>
                    </VisualState>
                </VisualStateGroup>
            </VisualStateGroupList>
        </Setter>
    </Style>
    <Style TargetType="telerik:RadDataPager" BasedOn="{StaticResource RadDataPagerStyle}" />

</ResourceDictionary>