<ContentPage
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:ios="clr-namespace:Microsoft.Maui.Controls.PlatformConfiguration.iOSSpecific;assembly=Microsoft.Maui.Controls"
    ios:Page.UseSafeArea="True"
    Title="{Binding AppTitle}"
    x:Class="SDKBrowserMaui.Pages.HomePage">

    <ListView x:Name="controls" ItemsSource="{Binding Controls}" SelectedItem="{Binding SelectedControl,  Mode=TwoWay}" >
        <ListView.ItemTemplate>

            <DataTemplate>
                <ViewCell>
                    <Grid ColumnSpacing="5" RowDefinitions="40" ColumnDefinitions="50,*">
                        <Image Source="{Binding Icon}" HorizontalOptions="Center" VerticalOptions="Center" Aspect="Fill" WidthRequest="16" HeightRequest="16" />
                        <Label Grid.Column="1" Text="{Binding Name}" VerticalOptions="Center" />
                    </Grid>
                </ViewCell>
            </DataTemplate>
        </ListView.ItemTemplate>
    </ListView>
</ContentPage>
